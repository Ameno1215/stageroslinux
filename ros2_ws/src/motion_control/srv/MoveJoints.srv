# Request
float64[] joints           # Tableau des valeurs cibles (en radians)

string[]  constrained_joints
float64[] joint_min
float64[] joint_max

bool is_relative           # True = ajoute ces angles aux angles actuels, False = va à ces angles absolus
bool execute               # True = planifie et bouge, False = planifie seulement


---
# Response
bool success
string message